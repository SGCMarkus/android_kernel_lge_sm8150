#
# LGE touch core configuration
#
if INPUT_TOUCHSCREEN

menuconfig LGE_TOUCH_STM_FTM4
	bool "LGE Touch Core ODM Device Driver - STM(FTM4)"
	depends on LGE_TOUCH_CORE_BASE
	help
	  If your device is STM(FTM4), say Y

endif
